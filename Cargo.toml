[package]
name = "iced_color_map"
version = "0.1.0"
edition = "2024"

[dependencies]
hex_color = { path = "../hex_color" }
iced = { version = "0.14", features = ["canvas"] }
iced_aw = { version = "0.13", features = ["color_picker"] }
